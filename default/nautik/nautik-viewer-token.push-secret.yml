---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: nautik-viewer-token
  namespace: default
spec:
  deletionPolicy: Delete
  refreshInterval: 10m0s
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: 1password-connect
  selector:
    secret:
      name: nautik-viewer-token
  data:
    - match:
        secretKey: token
        remoteRef:
          remoteKey: "homelab-argocd-k3s-test-cluster - default - nautik-viewer"
          property: "password"
...
