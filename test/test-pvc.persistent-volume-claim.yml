---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: test
  annotations:
    csi.proxmox.sinextra.dev/lifecycle: "delete"
spec:
  storageClassName: proxmox-data-xfs
  volumeName: test-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
...
